  MONGO_URI=mongodb+srv://deveshsoniwork_db_user:qkyCizDfKNirsRHJ@cluster0.zkgilef.mongodb.net/bookit?retryWrites=true&w=majority
  PORT=5000
  ```
- Save as:  
  `C:\Users\Devesh Soni\Assign\bookit-backend\.env`  
  (_File name must be `.env` with no extension!_)

---

## 2. **Fix Frontend React Error**

You may have run the install from the wrong directory.  
**Terminal commands to run, step by step:**
```powershell
cd "C:\Users\Devesh Soni\Assign\bookit-frontend"
Remove-Item -Recurse -Force node_modules
Remove-Item -Force package-lock.json
npm install react@19.1.1 react-dom@19.1.1 react-router-dom@6.23.0
npm install
npm run dev
```
*(Open PowerShell in that folder, and run these one-by-one—not as a single script.)*

---

## 3. **Start Backend**

```powershell
cd "C:\Users\Devesh Soni\Assign\bookit-backend"
npm install
npm run dev
```

---

## 4. **Recap**

- **Backend** connects to MongoDB Cloud, works via `.env` file.
- **Frontend** is reset and uses only one version of React, fixes hook call error.

---

**If you strictly follow these steps, you WILL get both backend and frontend running without errors. If any error remains, please copy-paste the exact error message as it appears.**